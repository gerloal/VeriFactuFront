@page
@model Verifactu.Portal.Pages.Account.SignedOutModel
@{
    ViewData["Title"] = "Sesión cerrada";
}

@section Styles {
    <style>
        .signedout-wrapper {
            min-height: 60vh;
            display: flex;
            align-items: center;
        }

        .signedout-icon {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            background: rgba(25, 135, 84, 0.12);
            color: #198754;
        }
    </style>
}

<div class="container signedout-wrapper py-5">
    <div class="row justify-content-center w-100">
        <div class="col-lg-6 col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center p-5">
                    <div class="signedout-icon mb-3">&#10004;</div>
                    <h1 class="h4 mb-2">Sesión cerrada correctamente</h1>
                    <p class="text-muted mb-4">
                        Cerraste la sesión el <strong>@Model.SignedOutAtDisplay</strong>.
                        Tus credenciales activas se han eliminado de forma segura.
                    </p>
                    <div class="d-flex flex-column flex-sm-row gap-2 justify-content-center">
                        <a asp-page="/Index" class="btn btn-primary">Volver al inicio</a>
                        <a asp-page="/Account/Login" asp-route-returnUrl="/" class="btn btn-outline-secondary">
                            Iniciar sesión de nuevo
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
